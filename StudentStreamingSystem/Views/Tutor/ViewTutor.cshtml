@model List<StudentStreamingSystem.Context.Tutor>
@{
    ViewBag.Title = "ViewTutor";
}
<h2 class="text-center">Tutor Details</h2>
<hr />
<h4>Upload Excel File</h4>
@using (Html.BeginForm("UploadExcelTutor", "Tutors", FormMethod.Post, new { enctype = "multipart/form-data", onsubmit = "return myFunction()" }))
{
    <div class="form-group">
        <div class="control-label row-cols-4">
            <input type="file" id="FileUpload" name="FileUpload" class="btn btn-outline-primary" style="border:1px solid blue" />
            <input type="submit" value="Upload" id="btnSubmit" class="btn btn-primary btn-sm" />
        </div>
    </div>
}
<hr />
<a href="@Url.Action("ViewTutor","Tutor")" class="btn btn-success btn-sm">Refresh</a>
<a href="@Url.Action("Create","Tutor")" class="btn btn-success btn-sm">Create New</a>
<table class="table table-hover table-bordered">
    <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Edit/Delete</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Name</td>
            <td>@item.Email</td>
            <td>
                <a href="@Url.Action("EditTutors",new {id=item.TutorID })" class="btn btn-success btn-sm">Edit</a>
                <a href="@Url.Action("DeleteTutors",new {id=item.TutorID })" class="btn btn-danger btn-sm">Delete</a>
            </td>
        </tr>
    }
</table>
<a href="@Url.Action("AdminUpload","Admin")" class="btn btn-primary">Back To Admin</a>

